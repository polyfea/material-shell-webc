# Uncomment to get SPA behavior with fallback to index.html
#
# <Directory ~ "^/[\w+\d+-]+">
#   FallbackResource /index.html
# </Directory>
# <Files ~ "\.(m?js|css|gif|jpe?g|png|svg|woff2?)$">
#   FallbackResource disabled
#   ErrorDocument 404 "File not found"
# </Files>